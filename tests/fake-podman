#!/usr/bin/env sh
set -eu

if [ "${1:-}" = system ] && [ "${2:-}" = connection ] && [ "${3:-}" = list ]; then
  printf 'red\nblue\n'
  exit 0
fi

if [ "${1:-}" = --connection ] && [ "${3:-}" = volume ] && [ "${4:-}" = inspect ]; then
  [ "${5:-}" = exists ] && exit 0
  exit 1
fi
printf '%s\n' "$*"
