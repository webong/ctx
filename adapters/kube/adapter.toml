api_version = "1"
name = "kube"
executable = "ctx-kube"
description = "First-party Kubernetes context adapter"
capabilities = "list,configure,validate,run,doctor"
selector_key = "kube_context"
extra_keys = "kube_namespace,kubeconfig"
commands = "kube,kubectl"
first_party = "true"
